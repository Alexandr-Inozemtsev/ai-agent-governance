# Scripts

В этой версии нет runtime scripts и внешних зависимостей. Папка содержит только псевдокод будущего hard validation.

Hard validator должен быть отдельным инструментом, который читает YAML/Markdown policy, классифицирует запрос и блокирует forbidden actions до выполнения.
